// ═══════════════════════════════════════════════════════════════
//  统一掉率配置中心
//  所有战斗/任务掉率集中在此，方便调整
// ═══════════════════════════════════════════════════════════════
var DROP_CONFIG = {
    // ─── 经验值 ───
    // 控制战斗胜利后获得的经验值
    xp: {
        base: 100,           // 基础经验（必得）
        bossBonus: 0,        // Boss额外经验（0表示不加）
    },

    // ─── Rout 掉落 ───
    // 控制战斗胜利后 Rout（游戏货币）的掉落概率和数量
    rout: {
        boss:     { chance: 0.15, amount: 2 },  // Boss战 15% 掉 2
        elite:    { chance: 0.10, amount: 3 },  // 精英战 10% 掉 3
        mechanic: { chance: 0.10, amount: 3 },  // 机械师 10% 掉 3
        normal:   { chance: 0, amount: 0 },  // 普通敌人不掉
    },

    // ─── 蓝图掉落 ───
    // 控制 Boss 战斗后蓝图的掉落概率（game-core.js 和 corpus_missions.js 均读取此配置）
    blueprint: {
        bossChance: 0.20,     // Boss战 20% 掉蓝图（1.0 = 100%，0.10 = 10%）
    },

    // ─── 卡片掉落 ───
    // 控制战斗胜利后战甲卡片的掉落概率和保底机制
    card: {
        baseChance: 0.35,        // 基础掉率 35%
        pityThreshold: 20,    // 保底：20战必掉
    },

    // ─── 材料掉落（常规肃清）───
    // 控制普通敌人掉落材料（矿石/合金等）的概率
    material: {
        normalChance: 0.40,   // 普通敌人 40% 掉材料
    },

    // ─── 恢复剂数值 ───
    // 控制各种恢复剂回复的负荷值
    stamina: {
        small:  50,   // 小型恢复剂
        medium: 100,  // 中型恢复剂
        large: 200,  // 大型恢复剂
        full:   'full', // 全满恢复剂（特殊处理）
        absoluteMax: 1000, // 绝对上限
    },
};
